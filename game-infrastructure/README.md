# Game Infrastructure

This directory will contain the server code and configuration for the Caller of Light Within RPG. It includes:

- Instructions for launching an EC2 instance and installing Node.js and dependencies.
- Steps to deploy the game backend and serve the frontend.
- Security group settings and optional Nginx reverse proxy configuration.
