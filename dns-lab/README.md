# DNS Lab

This directory documents experiments with configuring a local DNS server. It contains:

- Configurations for forward and reverse lookup zones.
- Examples of record types (A, CNAME, MX) and how to test DNS resolution.
- Notes on troubleshooting and networking fundamentals.
